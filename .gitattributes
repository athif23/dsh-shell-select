# Normalize to LF in the repository regardless of the contributor's platform.
# The CI matrix spans Windows, Linux, and macOS, so a checkout that converted
# line endings would show spurious diffs and fail formatting-sensitive checks.
* text=auto eol=lf

*.png binary
*.jpg binary
*.gif binary
